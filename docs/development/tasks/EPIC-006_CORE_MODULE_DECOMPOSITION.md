# EPIC-006 — Decompose the four largest modules

## Goal

Split `assistant.py` (4671 lines), `frontend/roadplanner-panel.js` (6745 lines), `roadplanner.py` (3605 lines) and `experience_manager.py` (3143 lines) into smaller, single-responsibility modules — no behavior change, no schema/data change. This is a planning document only; nothing here has been implemented. Each numbered step below should become its own small RP-XXX task, validated with the full check suite before moving to the next step.

## Why these four, and why in this order

All four are god-object/god-file patterns: one huge class (or, for the JS file, one huge `HTMLElement` subclass) that owns several genuinely separable responsibilities. None of them have circular-import problems severe enough to block a start, except `roadplanner.py` ↔ `changeset.py`, which is called out explicitly below.

Recommended file order (independent of the in-file step order within each): **`assistant.py` → `experience_manager.py` → `roadplanner.py` → `roadplanner-panel.js`**. The first two decompose cleanly into a facade + collaborators with low blast radius. `roadplanner.py` has one real circular-dependency knot (see below) that's easier to untangle once the pattern has been proven twice. `roadplanner-panel.js` is last because it needs a `panel.py` static-file change and a test-harness change (see its section) before any file can even be split — that's infrastructure work the other three don't need.

Every step must pass, before moving on:
```
python tools/release.py check
```
(Python 3.12 required — see the SessionStart hook merged earlier; `navigation.py` doesn't parse on 3.11.)

---

## 1. `assistant.py` (4671 lines) — do first

One class, `RoadplannerAssistant`, plus module-level basket/session machinery. Proposed split:

| New module | Responsibility | Depends on |
|---|---|---|
| `assistant_shared.py` | Pure text/ID sanitization + constants used by both basket and compile code (`_clean_text`, date regexes, `_ALLOWED_ENTITY_TYPES`, `_BASKET_ENTITY_ALIASES`) | stdlib only |
| `assistant_basket.py` | The conversational "change basket": `AssistantSession`/`AssistantSessionStore`, basket-item repair/normalization | `assistant_shared`, `canonical_day`, `roadplanner.ValidationError` |
| `assistant_compile.py` | Normalize a provider's raw "compile" JSON (alias/casing, new-day temp-IDs) into the canonical operation dialect + bounded Roadbook context | `assistant_shared`, `canonical_day`, `structured_output` |
| `assistant_operation_sanitizer.py` | Turn one compiled operation into a strictly-validated ChangeSet operation (day/stop ID inference, position bookkeeping) — the trickiest function (`_sanitize_operation`) moves here **whole, unmodified** | `assistant_shared`, `assistant_compile` |
| `assistant.py` (slimmed) | Orchestration: chat turn handling, briefing, drafts, compile→sanitize→ChangeSet | all of the above |

**Don't split `_sanitize_operation` internally** — it interleaves envelope stripping, per-entity validation, ID-existence checks and stateful position bookkeeping tightly enough that decomposing it further risks behavior changes. Move it whole; revisit later with dedicated test coverage if wanted.

**Test impact** (both are raw source-string/`ast` checks, not Python imports — no import graph to rewire, just two path constants):
- `tests/test_assistant_day_reference_normalization.py` — `ast.parse`s `assistant.py` to extract `_normalize_compiled_day_reference` by name → repoint to `assistant_compile.py`.
- `tests/test_day_plan_integrity_contract.py` — greps `assistant.py` text for `"position_state"` / `'operation["position"] = insert_at + 1'` → repoint to `assistant_operation_sanitizer.py`.

**Extraction order**: `assistant_shared.py` → `assistant_compile.py` → `assistant_basket.py` → `assistant_operation_sanitizer.py` (highest risk, do last, verify `async_prepare_review` end-to-end afterward).

---

## 2. `experience_manager.py` (3143 lines) — do second

One class, `RoadplannerExperienceManager`, already a clean composition-root candidate — nine cohesive method clusters, each becomes a collaborator; the class itself becomes a thin facade.

| New module | Responsibility |
|---|---|
| `experience_helpers.py` | Pure day/stop/geo/folder-name parsing helpers |
| `media_token_service.py` | Sign/validate HMAC media redirect tokens |
| `media_library_manager.py` | OneDrive scan/delta sync engine + photo→day/stop auto-assignment + media CRUD |
| `media_vision_curation.py` | Shared "local filter → optional Gemini Vision" engine (used by both media curation and destination galleries) |
| `media_curation_manager.py` | Per-stop/trip photo album curation, on top of `media_vision_curation` |
| `destination_gallery_manager.py` | AI-assisted planning-photo galleries per stop, background auto-population |
| `place_enrichment_orchestrator.py` | Place-enrichment preview/submit → ChangeSet (delegates resolution to existing `place_enrichment.py`) |
| `decision_manager.py` | AI-suggested decision cards: generate, enrich, select/transfer/archive/delete |
| `panel_payload_builder.py` | Assemble the aggregated `"experience"` panel payload — extract **last**, highest fan-in |
| `experience_manager.py` (slimmed) | Facade: wires collaborators together, every current public method becomes a one-line delegation |

**Circular-dependency fix**: almost every collaborator ends by calling `self.async_panel_payload(...)`. Instead of each new module importing `panel_payload_builder.py` directly, inject a `get_panel_payload` callable (and, for the two background jobs that also trigger media curation, an `on_media_changed` callable) from the facade's `__init__`. This keeps all concrete cross-imports inside the facade and gives a clean one-directional dependency graph.

**Public surface that must not break**: `panel.py` calls ~20 methods directly on `runtime.experience` (`async_prepare_place_enrichment`, `async_submit_place_enrichment`, `async_curate_stop_media`, `async_panel_payload`, etc.) and reads OneDrive/vision settings as attributes. `__init__.py` and `experience_http.py` also touch it directly. All of these must remain reachable as facade attributes/methods — this is the point of keeping `experience_manager.py` as a facade rather than deleting it.

**Test impact** (five source-string contract tests, no Python-import rewiring needed):
- `test_decision_enrichment_contract.py` → repoint to `decision_manager.py`
- `test_destination_refresh_contract.py`, `test_background_visual_enrichment_contract.py` → repoint to `destination_gallery_manager.py`
- `test_day_plan_integrity_contract.py` → repoint to `place_enrichment_orchestrator.py`
- `test_gemini_vision_contract.py` → spans **two** new files (`media_curation_manager.py` + `media_vision_curation.py` for one half, `destination_gallery_manager.py` for the other) — update to read/assert against both.

**Extraction order**: `experience_helpers.py` → `media_token_service.py` → `decision_manager.py` → `place_enrichment_orchestrator.py` → `media_library_manager.py` → `media_vision_curation.py` → `media_curation_manager.py` → `destination_gallery_manager.py` → `panel_payload_builder.py` (last).

**Progress**: all 9 steps done (`experience_helpers.py`, `media_token_service.py`, `decision_manager.py`, `place_enrichment_orchestrator.py`, `media_library_manager.py`, `media_vision_curation.py`, `media_curation_manager.py`, `destination_gallery_manager.py`, `panel_payload_builder.py`). `experience_manager.py` is at 419 lines (from 3143), fully a thin facade. `experience_manager.py`'s decomposition is complete.

**Refined design for steps 6-9** (worked out before implementing step 6, to avoid rework in 7-9):

- `media_vision_curation.py` → `VisionCurationEngine`: owns the vision settings (`media_curation_mode`, `media_vision_max_candidates/highlights/daily_limit`, `vision_enabled` property) and one public method `async_curate(...)` (renamed from `_async_semantic_curation`). Constructed with `hass`/`store`/`onedrive`/`provider`. The facade gets read-only properties delegating to it (same pattern as `media_library_manager`'s settings in step 5), since `panel.py` reads these as plain attributes on `runtime.experience`.
- `_find_stop` (currently sitting in the destination-gallery code range) is used by **both** step 7 and step 8 — move it to `experience_helpers.py` when extracting step 7, rather than duplicating it.
- `media_curation_manager.py` → `MediaCurationManager`: owns `_vision_lock`/`_vision_status` (their lifecycle belongs to the batch-curation orchestration, not the engine), calls into `VisionCurationEngine.async_curate`, uses the existing `get_panel_payload` callback pattern.
- `destination_gallery_manager.py` → `DestinationGalleryManager`: mirrors step 5 — owns its own `async_initialize`/`async_shutdown` (moves the destination-enrichment scheduling half out of the facade entirely), its own lock/status/unsub handles, and also calls into `VisionCurationEngine.async_curate`. Its periodic background job currently calls `self.async_auto_curate_media(...)` (step 7) directly — replace with an injected `trigger_vision_curation(trip_id)` callback, same shape as step 5's `on_media_changed`.
- `panel_payload_builder.py` (last): has the highest fan-in (reads settings/status from every other collaborator plus `geocoder`/`provider`/`store`/`hass`/`manager`). Since nothing calls back into it, give it these as **constructor parameters**, not a callback — the facade's `async_panel_payload` becomes a one-line delegation, so steps 3-8's callback wiring (`get_panel_payload=self.async_panel_payload`) needs zero changes.

---

## 3. `roadplanner.py` (3605 lines) — do third

Bottom layer: exception hierarchy, JSON/ID/validation primitives, trip/day/stop normalization, routing-metric helpers, projections, `TripState`, and the ~1940-line `RoadplannerStore` god class (init/migration, queries, CRUD mutations, transaction/snapshot/backup, changeset apply, context export).

| New module | Responsibility |
|---|---|
| `json_io.py` | Atomic, size-bounded JSON/text file I/O (no domain knowledge) |
| `identifiers.py` | ID generation + field validators (`ValidationError` and friends live here or in a small exceptions module) |
| `json_tree_validation.py` | Recursive bounded "details" JSON validation |
| `trip_documents.py` | `normalize_stop`/`normalize_day_document`/`normalize_trip_document` + schema-version constants |
| `routing_helpers.py` | Derive/invalidate/reconcile routing metrics, ferry/transport metadata |
| `trip_projections.py` | Bounded read-only projections (`_compact_trip/_day/_stop`, summaries) |
| `trip_state.py` | `TripState` dataclass |
| `trip_repository.py` | On-disk layout, schema migration on load, transaction/snapshot/backup machinery |
| `trip_queries.py` | Read-only query surface (`load_trip`, `get_trip_summary`, `search_stops`, ...) |
| `trip_mutations.py` | CRUD/revision-checked writes (`add_day`, `update_stop`, ...) |
| `changeset_operations.py` | Store-side changeset glue (`preview_changeset`, `apply_changeset`, ...) |
| `context_export.py` | Handoff-context JSON/Markdown generation (check for overlap with existing `handoff.py` before finalizing — may belong there instead) |
| `roadplanner.py` (slimmed) | `RoadplannerStore` as a composition/façade class; same public API, so `manager.py` needs no changes beyond import paths |

**The one real circular-dependency risk in this whole EPIC**: `changeset.py` already imports `TripState`, `normalize_day_document`, `_stable_id`, etc. from `roadplanner.py` at module load time, while `roadplanner.py`'s own changeset-calling methods (`preview_changeset`, `apply_changeset`, ...) locally re-import `changeset.py` **inside the method body** — a clear sign the original author already hit this cycle. Fix: `changeset.py` should import from `trip_documents.py`/`trip_state.py`/`identifiers.py` directly (not from `roadplanner.py`), so only `roadplanner.py`/`changeset_operations.py` imports `changeset.py`, one-directionally. Do this as part of extracting `changeset_operations.py` (second-to-last step).

**Widely-imported symbols** (`ValidationError`, `RoadplannerError`, `validate_identifier`, `TripState`) are used by 15-28 sibling modules — keep them importable at stable paths (re-export from `roadplanner.py` if needed for a transition period) so this doesn't turn into a repo-wide import-path rewrite in one commit.

**Test impact**: `test_day_plan_integrity_contract.py` (reads `roadplanner.py` for `reindex_explicit_positions` — repoint to `trip_mutations.py`), `test_place_enrichment_changeset.py` and `test_roadbook_sequence_normalization.py` (both dynamically load `roadplanner`/`changeset`/etc. by file path into a synthetic package — need the new module names added to their loader lists).

**Extraction order**: `json_io.py` → `identifiers.py` → `json_tree_validation.py` → `routing_helpers.py` → `trip_projections.py` → `trip_documents.py` → `trip_state.py` → `trip_repository.py` → `trip_queries.py` → `trip_mutations.py` (update `test_day_plan_integrity_contract.py` alongside this step) → `changeset_operations.py` (resolve the `changeset.py` cycle here; update the two dynamic-loader tests) → `context_export.py`.

**Progress**: steps 1-7 done (`json_io.py`, `identifiers.py`, `json_tree_validation.py`, `routing_helpers.py`, `trip_projections.py`, `trip_documents.py`, `trip_state.py`). `trip_state.py` holds the `TripState` dataclass plus `MAX_COORDINATOR_DAYS`/`MAX_COORDINATOR_STOPS` (moved wholesale, no other consumers). `changeset.py` imports `TripState` directly `from .roadplanner import TripState`, still satisfied by the re-export - this is the last of the widely-imported symbols called out in the "widely-imported symbols" note above still living as a plain module-level class rather than a forward reference. `trip_projections.py` holds `_compact_trip`/`_compact_day`/`_compact_stop`/`_media_from_details`/`_first_trip_media`, importing `_bounded_json_value` from `bounded_json.py` and `_routing_summary` from `routing_helpers.py` as anticipated in step 4's notes; it references `TripState` the same `TYPE_CHECKING`-guarded way `routing_helpers.py` does.

**Unified concept for steps 8-11** (`trip_repository.py`, `trip_queries.py`, `trip_mutations.py`, `changeset_operations.py`), worked out before starting step 8 to avoid rework, same discipline as the steps 6-9 concept in `experience_manager.py`'s decomposition. Unlike steps 1-7 (independent pure functions/dataclasses), `RoadplannerStore` is a genuine god-class where mutations, changesets and the one-time `initialize()` all funnel through the same commit/snapshot/transaction machinery - a naive per-method move would leave four collaborators each re-implementing "safe write" or awkwardly reaching back into each other.

- **`trip_repository.py` → `TripRepository`** (dataclass, same four fields as today's `RoadplannerStore`: `roadbook_dir`, `backup_dir`, `handoff_dir`, `backup_count`, so every existing method body's `self.roadbook_dir`/`self.pointer_path`/etc. keeps working unchanged - a pure copy-paste move with zero renames). Owns: `pointer_path`/`trips_dir` properties, `_load_pointer`, `_trip_dir`, `_day_path`, `_load_state`, `_assert_content_hash`, `_index_legacy_unmanaged_days`, `_default_trip_document`, `_transaction_marker_path`, `_write_state_transaction`, `_recover_transaction`, `_create_snapshot`, `_restore_snapshot`, `_prune_backups`, `create_backup`, the shared mutation-preamble validators `_check_expected_trip`/`_check_revision`/`_insert_index` (used by **both** future `TripMutations` and `ChangesetOperations`, so they belong on the shared repository, not either collaborator), and `_commit` (the core revision-bump/snapshot/transaction-write/reload primitive every mutation and `apply_changeset` calls).
  - **Key change to avoid a circular/awkward construction**: `_commit` currently ends by calling `self._write_context_best_effort(verified)` (context-writing doesn't exist as its own module yet) - but repository must not depend on the not-yet-extracted `context_export.py`. Fix: `_commit` returns `tuple[dict[str, Any], TripState | None]` instead of just `dict[str, Any]` - the `TripState` is `None` exactly when `business_value()` was unchanged (mirroring today's early-return branch that also skips the context write), otherwise it's the verified post-write state. Every caller (in `TripMutations`/`ChangesetOperations`) does `result, verified = self._repository._commit(...); if verified is not None: self._write_context_best_effort(verified)`. Same treatment for the repository half of `initialize()` (rename to `initialize_documents`, return the final `TripState` instead of its `coordinator_payload()`) - the facade's `initialize()` calls it, then calls the context collaborator, then builds the payload. `set_active_trip` (in `TripMutations`) doesn't go through `_commit` (it writes the pointer file directly) but needs the identical context-write callback, so the callback is injected into `TripMutations`, not wired through `_commit`'s return value alone.
- **`trip_queries.py` → `TripQueries(repository)`**: pure read-only surface - `load_trip`, `load_coordinator_payload`, `get_trip_summary`, `get_days`, `get_day`, `search_stops`, `list_trips`, `get_routing_plan`, `export_trip`. No context-writing, no injected callback needed.
- **`trip_mutations.py` → `TripMutations(repository, *, write_context_best_effort)`**: `apply_routing_results`, `set_active_trip`, `update_trip`, `add_day`, `update_day`, `remove_day`, `add_stop`, `update_stop`, `remove_stop`. `write_context_best_effort` is injected the same way `experience_manager.py`'s collaborators took `get_panel_payload`.
- **`changeset_operations.py` → `ChangesetOperations(repository, *, write_context_best_effort)`**: `preview_changeset`, `inspect_changeset_for_import`, `apply_changeset`, `adopt_external_changes` (classified here rather than `trip_mutations.py` since it's part of the same external-reconciliation family the panel calls `async_ingest_external_changeset` against, even though it doesn't go through `changeset.py` itself). **This is where the real circular-dependency fix from the plan above happens**: change `changeset.py`'s own top-of-file import from `from .roadplanner import (DAY_SCHEMA_VERSION, MAX_DAYS, MAX_STOPS_PER_DAY, TripNotFoundError, TripState, ValidationError, _stable_id, _validate_json_tree, _without_audit_fields, normalize_day_document, normalize_stop, normalize_trip_document, utc_now_iso, validate_identifier)` to pull each name from wherever it actually lives now (`identifiers.py`, `json_io.py`, `json_tree_validation.py`, `trip_documents.py`, `trip_state.py`) instead of from `roadplanner.py`. Once `changeset.py` no longer touches `roadplanner.py` at all, `changeset_operations.py` can do a normal top-level `from .changeset import changeset_summary, execute_changeset, normalize_changeset` and the three per-method local `from .changeset import (...)` workarounds disappear entirely.
- **`context_export.py` → `ContextExport(repository)`** (step 12, last): `_context_stop`, `_context_payload`, `_context_markdown`, `get_context_payload`, `get_context_markdown`, `write_context`, and `write_context_best_effort` (dropping the leading underscore since it's now called from other collaborators, not just internally - Python has no real privacy anyway). Needs `repository._load_state` and `repository.handoff_dir`, both already available once `TripRepository` exists.
- **Facade construction order** (in `RoadplannerStore.__post_init__`, since `@dataclass(slots=True)` needs every attribute declared as a field with `field(init=False)`): build `_repository` first, then `_context` (needs `_repository`), then `_queries`/`_mutations`/`_changesets` last (mutations/changesets need `_context.write_context_best_effort` already resolved to a bound method - no circular construction because the callback is handed over as a value *after* `_context` exists, never the reverse).
- **Facade surface**: every current public method on `RoadplannerStore` (verified against every `store.<method>` call site in `manager.py`) becomes a one-line delegation to the owning collaborator; `roadbook_dir`/`backup_dir`/`handoff_dir`/`backup_count` stay plain fields on the facade itself (`__init__.py` reads `store.handoff_dir` directly, and constructs `RoadplannerStore(roadbook_dir=..., backup_dir=..., handoff_dir=..., backup_count=...)` by keyword - both must keep working unchanged).
- **Test impact confirmed**: `test_day_plan_integrity_contract.py` will need `trip_mutations.py` added to its `roadplanner` source-string concatenation once `add_stop`/`update_stop` (containing `reindex_explicit_positions(target["stops"])`) move there. The two dynamic-loader tests need `"changeset_operations"` (and whichever of `trip_repository`/`trip_queries`/`trip_mutations`/`context_export` `changeset.py`'s new direct imports touch) is unnecessary to add explicitly - confirmed in steps 1-7 that the synthetic package's `__path__` resolves new submodules automatically - but worth a manual check after step 11 since that's the step that changes `changeset.py` itself.

**Step 8 done** (`trip_repository.py`), with one adjustment to the staged rollout: rather than building `TripQueries`/`TripMutations`/`ChangesetOperations` as separate classes *in this same step*, `RoadplannerStore` keeps all of its current public methods (queries, mutations, changesets, context export) verbatim for now and simply delegates the low-level primitives to `self._repository` - a `TripRepository` instance built in `__post_init__`. This isolated the risk of the `_commit` contract change (now returns `tuple[dict, TripState | None]`) to a single class (`RoadplannerStore` itself is still the only caller), verified by the full test suite, before steps 9-11 physically move chunks of its methods into their own collaborator classes. A `_commit_and_notify` helper on the facade wraps the common `result, verified = self._repository._commit(...); if verified is not None: self._write_context_best_effort(verified)` pattern used by 8 of the 9 `_commit` call sites (`apply_routing_results` needs the raw tuple directly, since it always does a second unconditional reload for route metrics regardless of whether anything changed - preserved exactly as before). `RoadplannerStore` stays `@dataclass(slots=True)` by declaring `_repository: TripRepository = field(init=False, ...)` as its own slot, built in `__post_init__`; `roadbook_dir`/`backup_dir`/`handoff_dir`/`backup_count` remain plain fields (both keyword construction and `store.handoff_dir` reads confirmed unchanged). No functional or behavior changes; full `python tools/release.py check` passes, no contract test needed repointing (the mutation methods with `reindex_explicit_positions` haven't moved out of `roadplanner.py` yet).

Steps 9-11 (`trip_queries.py`, `trip_mutations.py`, `changeset_operations.py`) now become simpler mechanical moves: pull the relevant methods out of `RoadplannerStore` into a class taking `repository` (and, for mutations/changesets, `write_context_best_effort` injected the same way `_commit_and_notify` already proves out) - the hard design work and the risky `_commit` contract change are already done and verified.

**Step 9 done** (`trip_queries.py`): `load_trip`, `load_coordinator_payload`, `get_trip_summary`, `get_days`, `get_day`, `search_stops`, `list_trips`, `get_routing_plan`, `export_trip` moved into `TripQueries(repository)` exactly as planned - purely mechanical since queries need no context-write callback. `MAX_SUMMARY_DAYS`/`MAX_SUMMARY_STOPS`/`MAX_SEARCH_RESULTS` moved wholesale (no other consumers, confirmed by repo-wide grep before removing). `list_trips`'s `self.trips_dir` became `self._repository.trips_dir` since `TripQueries` holds the repository, not the facade. `RoadplannerStore` gained a `_queries: TripQueries = field(init=False, ...)` slot, built in `__post_init__` right after `_repository`. No functional or behavior changes; full `python tools/release.py check` passes, no contract test needed repointing.

**Step 10 done** (`trip_mutations.py`): `apply_routing_results`, `set_active_trip`, `update_trip`, `add_day`, `update_day`, `remove_day`, `add_stop`, `update_stop`, `remove_stop` moved into `TripMutations(repository, *, write_context_best_effort)` exactly as planned, plus its own copy of the `_commit_and_notify` helper (small deliberate duplication rather than a shared base class for one 12-line method - `ChangesetOperations` will get its own copy too in step 11). `set_active_trip`'s `self.pointer_path` became `self._repository.pointer_path`. `RoadplannerStore` gained a `_mutations` slot built right after `_queries`, wired with `write_context_best_effort=self._write_context_best_effort` (the facade's own bound method - still valid since `__post_init__` runs after the class body is fully defined, regardless of the method's later position in the source). `apply_changeset` (staying on the facade until step 11) needed its own interim copy of `_commit_and_notify` since the shared one moved with the other mutations. **Import-cleanup lesson reinforced**: a plain single-line `grep "from .roadplanner import"` missed `changeset.py`'s multi-line parenthesized import entirely; switched to an AST-based repo-wide scan (`ast.walk` for `ImportFrom` nodes with `module == "roadplanner"`) before removing any re-export, which caught 8 names (`DAY_SCHEMA_VERSION`, `MAX_DAYS`, `MAX_STOPS_PER_DAY`, `TripNotFoundError`, `_without_audit_fields`, `normalize_day_document`, `normalize_stop`, `normalize_trip_document`) that would otherwise have broken `changeset.py` at import time - **use the AST scan, not a text grep, for every remaining step's re-export check**. Updated `test_day_plan_integrity_contract.py` to also read `trip_mutations.py` for the `reindex_explicit_positions(target["stops"]) >= 3` count assertion. No functional or behavior changes; full `python tools/release.py check` passes.

**Step 11 done** (`changeset_operations.py`) - **the one real circular-dependency risk from this whole EPIC, resolved**: `changeset.py`'s top-of-file import changed from `from .roadplanner import (DAY_SCHEMA_VERSION, MAX_DAYS, MAX_STOPS_PER_DAY, TripNotFoundError, TripState, ValidationError, _stable_id, _validate_json_tree, _without_audit_fields, normalize_day_document, normalize_stop, normalize_trip_document, utc_now_iso, validate_identifier)` to five separate imports from `.identifiers`/`.json_io`/`.json_tree_validation`/`.trip_documents`/`.trip_state` - the leaf modules that actually own each symbol now. `changeset.py` touches `.roadplanner` nowhere at all anymore, so `changeset_operations.py` imports `changeset_summary`/`execute_changeset`/`normalize_changeset` normally at module load time; the three per-method `from .changeset import (...)` workarounds in `preview_changeset`/`inspect_changeset_for_import`/`apply_changeset` are gone.

`ChangesetOperations(repository, *, write_context_best_effort)` holds `preview_changeset`, `inspect_changeset_for_import`, `apply_changeset`, `adopt_external_changes`, plus its own copy of `_commit_and_notify` as planned (mirrors `TripMutations`'s copy - `apply_changeset`'s interim facade-level copy from step 10 is gone now that `apply_changeset` itself moved).

**Re-running the AST-based re-export scan after this step found it had gone stale**: 8 of the 9 names step 10 confirmed as "still needed by `changeset.py`" (`DAY_SCHEMA_VERSION`, `MAX_DAYS`, `MAX_STOPS_PER_DAY`, `TripNotFoundError`, `_without_audit_fields`, `normalize_day_document`, `normalize_stop`, `normalize_trip_document`, plus `_stable_id` from earlier) were **only** needed because `changeset.py` used to import them from `.roadplanner` - once step 11 pointed `changeset.py` at the real leaf modules instead, none of these had any remaining external consumer, and all were dropped from `roadplanner.py`'s re-exports (none were used internally either, confirmed by grep). **Lesson**: a re-export confirmed necessary in one step can become dead the moment a later step changes its *only* consumer's import path - re-run the whole-repo AST scan fresh after any change to another module's imports, don't reuse a previous step's result.

One dynamic-loader test needed a real fix (not just a repoint): `test_roadbook_sequence_normalization.py` called `roadplanner.normalize_day_document(...)` directly on the loaded `roadplanner` module object - this breaks once `normalize_day_document` is no longer re-exported there. Fixed by adding `identifiers`/`json_io`/`json_tree_validation`/`trip_documents` to the test's synthetic-package loader list and calling `trip_documents.normalize_day_document(...)` instead. `test_place_enrichment_changeset.py` (the other dynamic-loader test) needed no changes - it only calls into `changeset.py`'s own public functions, which the `__path__`-based resolution already handles transparently for `changeset.py`'s new direct imports.

**All 11 of 12 planned modules for `roadplanner.py` are now extracted.** Only `context_export.py` (step 12, last) remains - `_context_stop`, `_context_payload`, `_context_markdown`, `get_context_payload`, `get_context_markdown`, `write_context`, `_write_context_best_effort` (dropping the leading underscore, since by the end of this step it's called from three other collaborators' constructors, not just internally).

**Step 12 done** (`context_export.py`) - **the `roadplanner.py` decomposition is complete: 3605 → 403 lines across 12 focused modules.** `ContextExport(repository)` holds `_context_stop`, `_context_payload`, `_context_markdown`, `get_context_payload`, `get_context_markdown`, `write_context`, and `write_context_best_effort` (renamed from `_write_context_best_effort` as planned, since it's called from `TripMutations`/`ChangesetOperations`/the facade's own `initialize()`, not just internally). `MAX_CONTEXT_DAYS`/`MAX_CONTEXT_STOPS_PER_DAY`/`MAX_CONTEXT_JSON_BYTES`/`MAX_CONTEXT_MARKDOWN_CHARS` moved wholesale (no other consumers). `write_context`'s `self.handoff_dir` became `self._repository.handoff_dir`.

`RoadplannerStore.__post_init__` now builds `_repository` → `_context` → `_queries`/`_mutations`/`_changesets`, with `_mutations`/`_changesets` wired to `self._context.write_context_best_effort` directly (no longer routed through a facade-level bound method, since `ContextExport` now exists at construction time). A fresh AST-based re-export scan after this step confirmed the final steady-state set of names `roadplanner.py` must keep re-exporting for sibling modules: `RoadplannerStore`, `RoadplannerError`, `ValidationError`, `StorageError`, `validate_identifier`, `_validate_json_tree`, `_read_json`, `_write_json_atomic`, `utc_now_iso`, `RevisionConflictError` - everything else (`_json_bytes`, `_write_text_atomic`, `canonical_order_stops`, `HANDOFF_CONTEXT_SCHEMA_VERSION`, `_compact_day`, `_compact_trip`, `_bounded_json_value`, `logging`/`_LOGGER`) had zero remaining consumers (internal or external) and was removed. No functional or behavior changes; full `python tools/release.py check` passes.

**Retrospective on the whole `roadplanner.py` decomposition**: the four "pure function/dataclass" steps (1-3, 5-7) were genuinely mechanical - extract, re-export, done. The two steps touching the actual `RoadplannerStore` god-class (8, then 9-12) needed the upfront "unified concept" design pass to avoid rework, specifically around the `_commit` return-contract change (step 8) that let every later collaborator decide independently whether to trigger a context-export refresh, without any of them depending on the not-yet-extracted `context_export.py`. The recurring risk across every step was **stale or incomplete re-export bookkeeping** - a plain grep missing a multi-line import, or a re-export confirmed necessary in one step going dead the moment a later step changed its only consumer's import path - caught every time by re-running a fresh AST-based whole-repo scan rather than trusting a previous step's result or a quick grep.

`trip_documents.py` (step 6) holds `normalize_stop`/`normalize_day_document`/`normalize_trip_document`, all four schema-version constants, `MAX_DAYS`/`MAX_STOPS_PER_DAY` (moved wholesale - still needed by not-yet-extracted `RoadplannerStore` methods and re-exported back), the `_ensure_*` field validators, `_validate_date_order`, `_safe_day_file`, `_known_and_legacy_details`, `_without_audit_fields`, `_business_day_document`. Two lessons from this step for the remaining ones: (1) some of these names (`normalize_stop`, `_without_audit_fields`, `_safe_day_file`, `_ensure_string`, `_ensure_optional_date`, `_ensure_non_negative_int`) are called directly by `RoadplannerStore` methods well outside the normalize_*-function bodies, not just internally - **grep every candidate name across the entire remaining `roadplanner.py`, not just the block being moved, before assuming it's a wholesale/no-consumer helper**; (2) `_stable_id` and `_validate_json_tree` have zero remaining call sites *inside* `roadplanner.py` after this move (both flagged "unused" by pyflakes there) but are still imported directly `from .roadplanner import ...` by `changeset.py`/`handoff.py`/`external_import.py` - **before removing a pyflakes-unused import from `roadplanner.py`, grep the whole repo for `from .roadplanner import <name>`, not just `roadplanner.py`'s own usage**, since pyflakes only sees one file at a time and can't catch a broken re-export. The exception hierarchy (`RoadplannerError`, `TripNotFoundError`, `ValidationError`, `StorageError`, `RevisionConflictError`, `ConcurrentModificationError`) ended up living in `json_io.py` rather than `identifiers.py`, since `json_io.py` needed it immediately and comes first in the extraction order (the plan's "or in a small exceptions module" caveat) - `identifiers.py` imports the exceptions from `json_io.py` rather than redefining them. `identifiers.py` holds `_new_id`, `_stable_id`, `validate_identifier`, `_ID_PATTERN`. `json_tree_validation.py` holds `_validate_json_tree` plus `MAX_STRING_LENGTH`/`MAX_DETAILS_DEPTH`/`MAX_DETAILS_ITEMS` (moved wholesale rather than left behind, since only `roadplanner.py`'s own remaining `_ensure_string`/`_ensure_text` still need `MAX_STRING_LENGTH` and re-export from there is simpler than a reverse import).

Two deviations introduced during step 4 (`routing_helpers.py`), both to avoid circular imports without inventing more modules than necessary: `utc_now_iso` (zero dependencies, needed by the routing invalidation helpers) moved into `json_io.py` rather than staying in `roadplanner.py`, alongside its existing exception/file-I/O primitives; and `_bounded_json_value` (used by routing metrics, the not-yet-extracted `_compact_trip/_day/_stop` projections, and handoff context export) got its own tiny `bounded_json.py` module rather than temporarily living in `routing_helpers.py` under a name that doesn't fit it - `trip_projections.py` and `context_export.py` (steps 5 and 12) will import it from there instead of owning it. `routing_helpers.py` itself references `TripState` only as a `TYPE_CHECKING`-guarded, string-quoted type hint (never resolved at runtime, thanks to `from __future__ import annotations`) to avoid needing `trip_state.py` (step 7) to exist yet - the functions only rely on `TripState`'s duck-typed shape (`ordered_days()`, `trip_document`, `day_documents`). `roadplanner.py` re-exports everything from all four modules unchanged, so no sibling-module import paths needed to change.

---

## 4. `frontend/roadplanner-panel.js` (6745 lines) — do last, needs infra first

One class, `RoadplannerPanel extends HTMLElement`, no existing mixin/module structure.

### Blocking infrastructure (must land before any file split)

1. **`panel.py` currently serves exactly one static file** (`async_setup_panel_support` registers a single `StaticPathConfig` mapping one URL to `roadplanner-panel.js`). Splitting into multiple files requires either serving the whole `frontend/` directory or registering one `StaticPathConfig` per new file.
2. **8 of 12 `tests/*.mjs` files load the file via `vm.runInThisContext(source)`** — a classic-script context that cannot execute `import`/`export`. These must switch to real dynamic `import()` of a `file://` URL (Node supports this natively) before ES modules can be introduced. The other 4 test files assert on raw substrings in the file text and need to be repointed to whichever new file now contains the string.

Recommendation: accept both infrastructure changes and use real ES modules (cleanest long-term), rather than working around them with runtime `<script>` injection or `new Function()` tricks.

**Infrastructure done**: both blockers above are resolved.

- `panel.py`'s `async_setup_panel_support` now registers the whole `frontend/` directory as one `StaticPathConfig` (`PANEL_STATIC_URL` → `frontend_dir`), instead of mapping a single specific file. `PANEL_MODULE_URL` dropped the baked-in `-{INTEGRATION_VERSION}.js` filename convention (the real on-disk filename, `roadplanner-panel.js`, is now used verbatim) since cache-busting was already handled separately by the `?v={INTEGRATION_VERSION}` query parameter `async_register_frontend_panel` appends to `module_url` - the versioned filename was redundant with it. Splitting the entry file into `lib/*.js`/`features/*.js` collaborators that import each other via relative paths (`./lib/constants.js`, etc.) now resolves correctly under the same served directory, with zero further changes to `panel.py` needed per new file.
- Added `frontend/package.json` (`{"private": true, "type": "module"}`). This only affects Node's module loader (used by the test suite's `import()` calls) - the browser already loads `roadplanner-panel.js` as an ES module via `panel_custom`'s `module_url` mechanism (`<script type="module">`-equivalent dynamic import), so it was unaffected either way. Without it, Node's `import()` would treat extensionless-package `.js` files as CommonJS by default, which cannot contain top-level `import`/`export` statements - needed once the entry file is actually split.
- All 8 `vm.runInThisContext(source)` test files (`test_assistant_interaction.mjs`, `test_assistant_links.mjs`, `test_canonical_stop_order.mjs`, `test_day_integrity_ui.mjs`, `test_destination_gallery.mjs`, `test_stop_reordering_ui.mjs`, `test_travel_integrity_ui.mjs`, `test_ux3_contract.mjs`) now do `await import(new URL(".../roadplanner-panel.js", import.meta.url))` instead of reading the source text and running it in a classic-script VM context. Every one of these files already set up its DOM/`customElements`/`HTMLElement` fakes on `globalThis` *before* the import - since `import()` only resolves once the module body finishes evaluating, side-effecting top-level code (`customElements.define(...)`) still runs at the same point relative to the fakes, so no behavior changed. The unused `node:fs/promises` `readFile` and `node:vm` imports were removed from all 8 files.
- The 4 raw-substring contract tests (`test_media_cover_ui.mjs`, `test_media_source_priority.mjs`, `test_place_enrichment_ui.mjs`, `test_vision_media_ui.mjs`) were left untouched here - they still read the monolithic file's text directly, and are the ones that will need repointing to specific new files once features actually move, per file, during the extraction steps below.
- `python tools/release.py check` passes with this infra in place and zero files split yet, confirming the plumbing works before any risky content move.

**`lib/styles.js` done** (first extraction step, per the recommended order - "zero logic, free win"): moved the `_styles()` method's ~880-line static CSS template literal (no `${...}` interpolation, confirmed by grep before moving) verbatim into `frontend/lib/styles.js` as `export const PANEL_STYLES = ...`. `roadplanner-panel.js` gained its first top-of-file `import { PANEL_STYLES } from "./lib/styles.js";` and the call site `this._styles()` became `PANEL_STYLES`; the now-empty `_styles()` method was deleted. `roadplanner-panel.js`: 6749 → 5863 lines. One of the 4 raw-substring contract tests, `test_place_enrichment_ui.mjs`, asserted on two CSS rules (`font-family: Roboto, Sans-Serif`, `font-weight: 400`) that moved with the style block - repointed by concatenating `roadplanner-panel.js` + `lib/styles.js` source text before the substring checks, matching the pattern the Python contract tests already use for multi-file source-string concatenation. `node --check` passes on both files; full `python tools/release.py check` passes.

**`lib/constants.js` and `lib/core-helpers.js` done** (next two steps, taken together since both are pure top-level `const` declarations with no interleaving): moved `WS_GET_DATA`/`WS_ACTION` plus all six label/icon dictionaries (`operationLabels`, `statusLabels`, `archiveDocumentTypeLabels`, `archiveExpenseCategoryLabels`, `archiveStatusLabels`, `stopIcons`) into `lib/constants.js`, and the five pure formatting/ID helpers (`escapeHtml`, `cleanText`, `newClientRequestId`, `nullableNumber`, `cloneObject`) into `lib/core-helpers.js` - both wholesale, no renames, each declaration confirmed single-definition and reference-only (no reassignment) via a repo-wide grep before moving. `roadplanner-panel.js` imports both modules at the top, alongside the already-existing `lib/styles.js` import. `roadplanner-panel.js`: 5863 → 5720 lines. No test repointing needed - none of the 4 raw-substring contract tests asserted on this content. `node --check` passes on all three `lib/*.js` files; full `python tools/release.py check` passes.

**First feature mixin done: `features/universal-import.js`** (dispatcher-Step-1 approach - `_handleClick`/`_handleChange` stay in core, only method *bodies* move). Universal Import turned out not to be physically contiguous in the source (its methods were interleaved with Archive's, not grouped together) - confirmed by grep before moving, so this was a method-by-method mixin extraction rather than a block move like the `lib/*` steps: `_importDocuments`, `_universalImport`, `_analyzeUniversalImport`, `_openUniversalImport`, `_renderUniversalImport`, `_renderUniversalImportCard`, `_renderUniversalImportReview` moved verbatim (object-literal method shorthand is syntactically identical to class-method shorthand, so no rewriting of the method bodies themselves was needed) into `export const universalImportMixin = { ... }`, applied via `Object.assign(RoadplannerPanel.prototype, universalImportMixin)` right after the class body, before `customElements.define`. `_importDocuments` stayed with this feature (it's the universal-import-specific document query) even though one of its three call sites is a tab-badge count read from core render code - safe, since mixin methods land on the same shared prototype regardless of which module defined them. `escapeHtml` imported directly from `lib/core-helpers.js`; every other reference (`this._archiveData`, `this._canEdit`, `this._statCard`, `this._renderReadOnlyNotice`, `this._renderModalHeader`, `this._runAction`, `this._loadData`, `this._render`) stays a plain `this.` call, resolved at runtime via the prototype chain exactly as before. `roadplanner-panel.js`: 5720 → 5630 lines (net; some of the drop is blank-line cleanup left behind by the extraction, collapsed via a repo-local blank-run normalization pass). No existing `.mjs` test exercised these methods directly, so a manual Node smoke script (import the module, drive `_importDocuments`/`_renderUniversalImport`/`_renderUniversalImportCard`/`_renderUniversalImportReview` against a fake `_data`) confirmed the mixin attaches and behaves identically before committing - not added as a permanent test file, since the EPIC's scope is structural extraction, not new test coverage. `node --check` passes on both files; full `python tools/release.py check` passes.

**Second feature mixin done: `features/place-enrichment.js`** (marked cross-cutting with the assistant in the plan, but its 3 methods - `_preparePlaceEnrichment`, `_submitPlaceEnrichment`, `_renderPlaceEnrichment` - had no assistant-specific dependencies beyond generic `this.` calls already staying in core, so no extra wiring was needed). Moved verbatim into `export const placeEnrichmentMixin = { ... }`, applied via `Object.assign(RoadplannerPanel.prototype, placeEnrichmentMixin)` alongside the universal-import mixin. Needed its own top-of-file `import { escapeHtml, cleanText } from "../lib/core-helpers.js"` (both used throughout `_renderPlaceEnrichment`'s candidate-card rendering). `roadplanner-panel.js`: 5630 → 5391 lines. `test_place_enrichment_ui.mjs` (one of the 4 raw-substring contract tests) needed repointing again - added `features/place-enrichment.js` to its concatenated source alongside `roadplanner-panel.js` and `lib/styles.js`, since the `"prepare_place_enrichment"`/`"submit_place_enrichment"` action-name strings and the candidate-card markup moved with the methods. `node --check` passes on both files; full `python tools/release.py check` passes.

**Third feature mixin done: `features/archive.js`** (51 methods - by far the largest single extraction so far, across 4 non-contiguous source regions: document/expense/todo data lookups + upload/clipboard/paste/offline-cache machinery around line 1230, day/stop archive-record label helpers around line 3675, the archive tab + card renderers around line 4045, and the archive dialog/form renderers around line 4846). Each region was split into its individual top-level methods (not moved as one raw text block, unlike the smaller single-method mixins) since object-literal methods need commas between entries - the first attempt at this step produced a `SyntaxError` from exactly that gap and was reverted before re-doing it method-by-method. **`_archiveSelect`/`_archiveCheckbox` deliberately stayed in core** despite their name: a repo-wide grep found both are also called from the not-yet-extracted media (`_renderMediaEdit`) and trip/day/stop (`_renderStopDialog`) rendering code - moving them would have broken those call sites until their own future extraction steps. `_archiveDayOptions`/`_archiveStopOptions`/`_archiveSelectedLink` right next to them *did* move, since every one of their call sites is archive-only. `_formatBytes` and `_formatMoney` also moved (both confirmed archive-only by grep, despite generically-sounding names) - `_formatMoney` sits right before the core `_handleChange` dispatcher, confirming the block boundary. `roadplanner-panel.js`: 5391 → 4598 lines. No test repointing needed - none of the 4 raw-substring contract tests assert on archive-specific content. `node --check` passes on both files; full `python tools/release.py check` passes.

**Fourth feature mixin done: `features/media.js`** (34 methods across 7 non-contiguous regions: the destination-gallery auto-populate job, the image-search dialog, destination-gallery data/rendering, experience-media-by-id lookups, the media tab + card renderers, the generic image-gallery/destination-image renderers, and the OneDrive/media-edit/destination-gallery/media-gallery dialog renderers). Same per-method split-and-join approach as `archive.js`, since these regions also contain multiple methods each.

**What stayed in core, and why** (all confirmed by grep before moving, same discipline as the `_archiveSelect` lesson): `_experienceData`/`_experiencePresentation` (called directly from inside the core `_handleClick` dispatcher body itself, not just from feature rendering); `_mediaFrom`/`_detailsWithMedia` (the latter is called directly from the core `_handleSubmit` dispatcher for the day/stop manual-image-URL form fields); `_tripImages`/`_tripCoverImage`/`_dayCoverImage`/`_experienceCoverForStop` (called from the not-yet-extracted trip/day/stop overview renderers - `_renderOverview`/`_renderDayRoute`/`_renderStopCard`/`_renderTotalRoute` - and judged tightly-enough coupled to "which cover image for this entity" that they should migrate together with `trip-day-stop.js`/`route-map.js` later rather than being pre-emptively split into media now).

**One real bug caught by the test suite, not by review**: the extraction script's method-exclusion list (used to keep `_experiencePresentation` in core) only excluded it from being *written into* `media.js` - it did not stop the same script's region-deletion step from *also deleting* it from `roadplanner-panel.js`, since deletion operated on the whole line range regardless of the per-method exclusion list. Every caller of `this._experiencePresentation()` (four call sites in the cover-image helpers above) started throwing `TypeError: ... is not a function` at runtime - caught immediately by `test_day_integrity_ui.mjs` failing during `python tools/release.py check`, not by `node --check` (which only validates syntax, not that every referenced method still exists). Fixed by re-inserting the method definition verbatim right after `_experienceData`. **Lesson for any future region-based extraction with an exclusion list**: an excluded name needs to be excluded from *both* the write step and the delete step, or explicitly re-inserted afterward - a syntax check alone cannot catch a valid-but-now-undefined method reference.

Four raw-substring/text-slurping contract tests needed repointing to add `features/media.js` to their concatenated source, since the moved methods' markup/strings moved with them: `test_background_visual_enrichment_contract.py` (Python; repointed from `roadplanner-panel.js` entirely to `media.js`, since the method it inspects moved wholesale), `test_media_cover_ui.mjs`, `test_place_enrichment_ui.mjs` (needed a second file added, now reads `roadplanner-panel.js` + `lib/styles.js` + `place-enrichment.js` + `media.js`), and `test_vision_media_ui.mjs`. `roadplanner-panel.js`: 4598 → 4058 lines. `node --check` passes on both files; full `python tools/release.py check` passes.

**Fifth feature mixin done: `features/decisions-integrity.js`** (9 methods, the smallest feature mixin so far - decisions and travel-integrity share this file since neither is large enough alone to justify separate files, matching the EPIC's own grouping). `_createDecisionFromMessage`, `_integrityData`, `_renderDecisions`, `_renderDecisionOptionGallery`, `_renderDecisionCard`, `_integrityStatusLabel`, `_integrityDimensionLabel`, `_renderIntegrityCard`, `_renderTravelIntegrity` moved; `decision_select_option`/`decision_transfer`/`decision_archive` stayed as inline `_runAction` calls inside the core `_handleClick` dispatcher (no wrapper methods existed for them to move). `_integrityData` is also called from the not-yet-extracted `_renderOverview` - left there per the by-now-established pattern (core dispatcher/not-yet-extracted-renderer callers don't block a move, since prototype-shared mixins resolve `this.` calls regardless of file, but a directly-dispatcher-called helper stays put). `roadplanner-panel.js`: 4058 → 3871 lines.

Three source-string contract tests needed repointing to add `features/decisions-integrity.js` to their concatenated source: `test_day_plan_integrity_contract.py` (`data-action="integrity-prepare-locations"` moved with `_renderTravelIntegrity`), `test_decision_baseline.py` (`"Bestehender Plan"`/`"Keine Änderung nötig"` moved with `_renderDecisionCard`). `node --check` passes on both files; full `python tools/release.py check` passes.

**Sixth feature mixin done: `features/assistant.js`** (29 methods across 5 non-contiguous regions: GPS-completion draft prep, assistant link/markdown rendering, the chat-submit/send/briefing/diagnostics/prepare-review machinery, autonomy/health labels + the chat tab renderers, and the draft/diagnostics dialog renderers). Same per-region method-split approach as `archive.js`/`media.js`. `roadplanner-panel.js`: 3871 → 3137 lines.

**No test repointing needed at all this time** - unlike every previous feature step. `test_assistant_interaction.mjs` and `test_assistant_links.mjs` (2 of the 8 `import()`-based tests) exercise assistant behavior but always loaded the *whole* `roadplanner-panel.js` module graph via dynamic `import()`, which transitively pulls in `features/assistant.js` through its own import - so they kept working with zero changes. Only the four *raw-substring* contract tests (which read a hardcoded file path's text directly, bypassing the module graph) have ever needed repointing in this EPIC; anything reachable through an actual `import()` chain is immune by construction. `node --check` passes on both files; full `python tools/release.py check` passes.

**Seventh and eighth (final) feature mixins done: `features/route-map.js` and `features/trip-day-stop.js`** together in one step, since the plan's own extraction order treats them as a matched pair extracted last. 25 methods moved to `route-map.js` (day/trip route calculation triggers, coordinate/route-point derivation, route-geometry/segment-path building, route-status/coverage labels, the external-map-link helper, the map/route-flow/total-route/route-graphic renderers, and the Leaflet map lifecycle: `_ensureHaMap`/`_buildLeafletLayers`/`_hydrateMaps`). 25 methods moved to `trip-day-stop.js` (stop-ordering logic, canonical day/stop derivation, the trip-select/overview/day-route/stop-card/total-day/trips-list renderers, the stop-order and trip/day/stop form/dialog renderers).

**What stayed in core, confirmed by the doc's own description of the Core group** ("shared formatters/permission checks (`_canEdit`, `_formatDate`, `_findDay`, ...) and the pervasive `_runAction`/`_render`/`_showToast`/`_closeDialog`"): the constructor/lifecycle/WS-send/load machinery, `_runAction`, `_findDay`/`_findStop`, the four permission checks, all formatters, `_safeUrl`, `_mediaFrom`/`_detailsWithMedia` (used directly by the core `_handleSubmit` dispatcher), the cover-image resolvers (`_tripImages`/`_tripCoverImage`/`_dayCoverImage`/`_experienceCoverForStop`, kept with core per the media.js step's reasoning), `_experienceData`/`_experiencePresentation`, `_handleChange`/`_handleClick`/`_handleSubmit` (the dispatcher itself - Step 1 of the plan's two-step dispatcher approach, kept as-is; the action-map conversion is Step 2, explicitly out of scope for this pass), `_render`/`_renderApp`/`_renderTabs`/`_renderActiveTab`/`_renderLoading`/`_renderError`/`_renderReadOnlyNotice`, `_renderToast`/`_renderDialog` (the dialog-type dispatcher), the generic dialog/form helpers (`_renderModalHeader`/`_renderActionErrorDialog`/`_renderConfirmDialog`/`_archiveSelect`/`_archiveCheckbox`/`_field`/`_hiddenField`/`_textarea`/`_selectField`/`_formActions`), and the handoff renderers (`_previewHandoff`/`_renderHandoffs`/`_renderHandoff`/`_renderHandoffPreview` - handoffs were never one of the plan's 7 named groups, so they stay put rather than being force-fit into one).

`roadplanner-panel.js`: 3871 → 2092 lines - inside the file-size range of the other already-decomposed core modules in this EPIC (`roadplanner.py` finished at 403, `experience_manager.py` at 419).

Five raw-substring/text-slurping contract tests needed repointing to add the two new files to their concatenated source (the same "add the file where the moved string now lives" pattern used throughout this EPIC, restated once more for completeness): `test_day_plan_integrity_contract.py`, `test_gemini_vision_contract.py`, `test_media_cover_ui.mjs`, `test_place_enrichment_ui.mjs`, `test_vision_media_ui.mjs`. One test needed a structural fix rather than just an added file: `test_media_source_priority.mjs` sliced `roadplanner-panel.js` text between `_renderStopCard(...)` and `_renderTotalRoute()` to isolate one method's body - since those two methods are now in different files entirely (`trip-day-stop.js` and `route-map.js` respectively), the slice was repointed to `trip-day-stop.js` alone with its new next-door neighbor `_renderTotalDay(day)` as the end marker.

**One real bug, caught immediately by the test suite**: `_effectiveDayStops` (moved to `trip-day-stop.js`) calls `cloneObject`, which the initial import line for that file omitted (only `escapeHtml`/`cleanText`/`stopIcons` had been grepped for) - `node --check` didn't catch it (syntax is valid; the identifier is simply undefined at runtime), but `test_canonical_stop_order.mjs` failed immediately with `ReferenceError: cloneObject is not defined` the first time that code path actually executed. Fixed by adding `cloneObject` to the file's `lib/core-helpers.js` import. **Reinforces the EPIC-wide lesson already noted for the Python decomposition**: a per-file identifier scan has to be exhaustive (every name actually referenced, not just the ones remembered from a similar earlier step), and only real execution - not `node --check` alone - proves a moved method's free variables all still resolve.

`node --check` passes on all 9 files (`roadplanner-panel.js` + 8 `features/*.js`); full `python tools/release.py check` passes with zero failures.

**This completes all 7 planned feature-mixin extractions from the frontend decomposition plan** (`universal-import.js`, `place-enrichment.js`, `archive.js`, `media.js`, `decisions-integrity.js`, `assistant.js`, `route-map.js` + `trip-day-stop.js`), on top of the 3 `lib/*.js` infrastructure modules and the panel.py/test-harness blocking-infrastructure step that preceded them. `roadplanner-panel.js` itself is now a thin core: lifecycle, the two dispatchers (`_handleClick`/`_handleChange`/`_handleSubmit`), generic render/dialog/form plumbing, and the handful of cross-cutting data accessors every feature mixin depends on. The only remaining item from the original plan is the **dispatcher action-map conversion** (Step 2: turning `_handleClick`'s ~700-line if/else chain into a per-feature action-map merged from each feature module) - deliberately left as a separate future step per the plan's own phrasing ("do this only after all features are isolated... the more thorough follow-up, touches every action name at once, highest blast radius of the whole EPIC"), and per the plan's still-open question of whether it's worth doing at all versus leaving the mixin-based split as the final state.

### Proposed groups (mixins applied via `Object.assign(RoadplannerPanel.prototype, ...)`, or later true action-map dispatch — see below)

| Group | File | Contents |
|---|---|---|
| Core | `lib/constants.js`, `lib/styles.js`, `lib/core-helpers.js` | WS constants, label dictionaries, `escapeHtml` etc.; the ~900-line CSS template string; shared formatters/permission checks (`_canEdit`, `_formatDate`, `_findDay`, ...) and the pervasive `_runAction`/`_render`/`_showToast`/`_closeDialog` |
| Assistant chat | `features/assistant.js` | Chat send/briefing/diagnostics, message rendering, link/markdown rendering |
| Universal import | `features/universal-import.js` | Upload/analyze/transfer/discuss/discard + rendering |
| Archive/documents | `features/archive.js` | Upload/clipboard/paste machinery, document/expense/todo CRUD + rendering |
| Media/gallery | `features/media.js` | OneDrive media, destination galleries, image search |
| Trip/day/stop + route/map | `features/trip-day-stop.js`, `features/route-map.js` | Stop/day CRUD, route calculation, map rendering — largest, most test coverage, extract last among features |
| Place enrichment | `features/place-enrichment.js` | Review-card rendering + prepare/submit (shared with assistant — flag as cross-cutting) |
| Decisions/integrity | `features/decisions-integrity.js` | Decision cards, travel-integrity rendering |

**The dispatch problem**: all clicks/changes are handled by one delegated listener calling two monolithic functions, `_handleClick` (~700 lines) and `_handleChange`, each a single long `if/else if` chain across every feature area. Two options:
- **Step 1 (do this first, lower risk)**: keep the dispatcher as-is in core, move method *bodies* out as mixins applied to the shared prototype.
- **Step 2 (do this only after all features are isolated)**: convert the dispatcher itself into a per-feature action-map lookup (`{"archive-upload": handler, ...}` merged from each feature module) — the more thorough follow-up, touches every action name at once, highest blast radius of the whole EPIC.

**Extraction order**: infra (panel.py static paths + test-harness `import()` switch) → `lib/styles.js` (zero logic, free win) → `lib/constants.js` → `lib/core-helpers.js` → smallest self-contained feature (universal-import or place-enrichment) → archive → media → decisions/integrity → assistant → trip/day/stop + route/map (largest, most test coverage, last) → only then the dispatcher action-map refactor (step 2 above), as its own dedicated final step.

---

## Open questions

- `context_export.py` (roadplanner.py split) vs. folding into existing `handoff.py` — needs a judgment call during implementation, not resolvable by static analysis alone.
- Whether `roadplanner-panel.js`'s eventual per-feature action-map dispatcher is worth doing in this EPIC at all, or left as a follow-up once the mixin-based split has proven itself.

## Status

All four planned decompositions are complete: `assistant.py`, `experience_manager.py`, `roadplanner.py`, and `frontend/roadplanner-panel.js` (6749 → 2092 lines, split across 3 `lib/*.js` modules and 8 `features/*.js` mixins, applied via `Object.assign(RoadplannerPanel.prototype, ...)`). Every step was validated with the full `python tools/release.py check` suite before moving to the next. The one deliberately-deferred item is the `roadplanner-panel.js` dispatcher action-map conversion (Step 2 of that file's plan) - left as an explicit future follow-up, not required to consider this EPIC's decomposition goal met.
